import React, { useEffect, useState } from \"react\";\nimport TelemetryBoard from \"@/components/TelemetryBoard\";\nimport TerminalLogs from \"@/components/TerminalLogs\";\n\nexport default function MissionControlPage() {\n  const [latency, setLatency] = useState(0);\n  useEffect(() => { const id=setInterval(()=>setLatency(Math.round(Math.random()*30)),5000); return()=>clearInterval(id); }, []);\n  return (\n    <div style={{background:'#13161A',color:'#fff',minHeight:'100vh',fontFamily:\"'Inter Variable'\",fontWeight:510,letterSpacing:'-1.056px'}}>\n      <header style={{padding:'1rem 2rem',borderBottom:'1px solid #222',display:'flex',justifyContent:'space-between',alignItems:'center'}}>\n        <div>System Status: <strong>OPERATIONAL</strong></div>\n        <div>Latency: {latency} ms</div>\n        <div style={{color:'#00D980'}}>AI ORCHESTRATION ACTIVE</div>\n      </header>\n      <main style={{padding:'2rem'}}>\n        <TelemetryBoard />\n        <section style={{marginTop:'2rem'}}>\n          <h2 style={{color:'#00D980'}}>Terminal / Log Interface</h2>\n          <TerminalLogs />\n        </section>\n        <div style={{marginTop:'2rem'}}><a href=\"/\" style={{color:'#00D980'}}>← Return to Home</a></div>\n      </main>\n    </div>\n  );\n}\n